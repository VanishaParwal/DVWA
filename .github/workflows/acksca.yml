name: AccuKnox Container Scan

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  container-scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run AccuKnox Container Scanner
        uses: accuknox/container-scan-action@latest
      
        with:
          accuknox_token: ${{ secrets.ACCUKNOX_TOKEN }}
          accuknox_label: ${{ secrets.ACCUKNOX_LABEL }}
          accuknox_endpoint: ${{ secrets.ACCUKNOX_ENDPOINT }}
          image_name: "nginx"
          tag: "latest"
          severity: "UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL"
          soft_fail: true
          upload_results: true  # (Optional) set true if you want to upload result to Github artefact
          generate_sbom: true   # (Optional) set true to generate SBOM
          project_name: "v-test"      # (Optional) must match the project name created in the dashboard
          dockerfile_context: Dockerfile  # (Optional) Path to Dockerfile for building image before scan 
